import 'package:flutter/material.dart';

const kmenuTextStyle = TextStyle(fontSize: 24.0, color: Colors.white);
const kmenuIconStyle = { };
const sizeboxHeight = 10.0;
const iconSize = 50.0;
const colorIconMenu = Colors.white;